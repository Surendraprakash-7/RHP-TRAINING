#include<iostream>
#include<algorithm>
using namespace std;
int main(){
   int n;
   cin>>n;
   int h[n],l[n];
   for(int i=0;i<n;i++)
   cin>>h[i]>>l[i];
   int q;
   cin>>q;
   int t[q];
   for(int i=0;i<q;i++)
   cin>>t[i];
   int y[n];
   y[n-1] = h[n-1];
   for(int i=n-2;i>=0;i--){
       if(h[i]>y[i+1])
       y[i] = h[i];
       else
       y[i]=y[i+1];
   }
   for(int i=0;i<q;i++){
   int e = upper_bound(l,l+n,t[i])-l;
   cout<<y[e]<<endl;
   }
}
